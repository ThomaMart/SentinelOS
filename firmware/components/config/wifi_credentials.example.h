#ifndef WIFI_CREDENTIALS_H
#define WIFI_CREDENTIALS_H

/* TEMPLATE — copy this file to  firmware/secrets/wifi_credentials.h  (git-ignored)
 * and fill in your own network credentials. The secrets/ path is provisioned
 * locally and never committed, so real Wi-Fi credentials stay out of git. */

#define SENTINELOS_WIFI_SSID     "your-ssid"
#define SENTINELOS_WIFI_PASSWORD "your-password"

#endif /* WIFI_CREDENTIALS_H */
